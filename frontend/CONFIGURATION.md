# Конфигурация веб-приложения YouDovezu

## Обзор

Веб-приложение YouDovezu использует конфигурационную систему для управления настройками для разных окружений (разработка, тестирование, продакшен).

## Структура конфигурации

### 📁 Файлы конфигурации

```
frontend/
├── src/
│   └── config.ts          # Основной конфигурационный файл
├── env.example            # Пример переменных окружения
├── env.testing           # Конфигурация для тестирования
├── env.production        # Конфигурация для продакшена
└── package.json          # Скрипты сборки
```

### ⚙️ Основной конфигурационный файл (`src/config.ts`)

```typescript
export const config = {
  // URL API сервера
  apiBaseUrl: process.env.REACT_APP_API_URL || 'http://localhost:8080',
  
  // URL веб-приложения
  webAppUrl: process.env.REACT_APP_WEBAPP_URL || 'http://localhost:3000',
  
  // Окружение
  environment: process.env.NODE_ENV || 'development',
  
  // Дополнительные настройки
  requestTimeout: 10000,
  enableLogging: process.env.NODE_ENV === 'development',
  testMode: process.env.REACT_APP_TEST_MODE === 'true',
};
```

## Переменные окружения

### 🔧 Основные переменные

| Переменная | Описание | Пример |
|------------|----------|---------|
| `REACT_APP_API_URL` | URL API сервера | `http://localhost:8080` |
| `REACT_APP_WEBAPP_URL` | URL веб-приложения | `http://localhost:3000` |
| `REACT_APP_TEST_MODE` | Режим тестирования | `true`/`false` |
| `NODE_ENV` | Окружение | `development`/`production` |

### 📋 Файлы конфигурации по окружениям

#### Разработка (по умолчанию)
```bash
# env.example
REACT_APP_API_URL=http://localhost:8080
REACT_APP_WEBAPP_URL=http://localhost:3000
REACT_APP_TEST_MODE=false
NODE_ENV=development
```

#### Тестирование
```bash
# env.testing
REACT_APP_API_URL=https://api-test.youdovezu.com
REACT_APP_WEBAPP_URL=https://app-test.youdovezu.com
REACT_APP_TEST_MODE=true
NODE_ENV=development
```

#### Продакшен
```bash
# env.production
REACT_APP_API_URL=https://api.youdovezu.com
REACT_APP_WEBAPP_URL=https://app.youdovezu.com
REACT_APP_TEST_MODE=false
NODE_ENV=production
```

## Использование в коде

### 🔌 Импорт конфигурации

```typescript
import { apiConfig, getInitData, log } from './config';
```

### 🌐 API запросы

```typescript
// Использование конфигурации для API запросов
const response = await axios.post(
  apiConfig.getUserInfo(), 
  formData, 
  apiConfig.axiosConfig
);
```

### 📝 Логирование

```typescript
// Логирование (только в development)
log('User info received:', response.data);
```

### 🔐 Получение initData

```typescript
// Автоматическое получение initData
const initData = getInitData();
```

## Скрипты сборки

### 🚀 Доступные команды

```bash
# Разработка
npm start                    # Обычный режим разработки
npm run start:testing       # Режим тестирования

# Сборка (Windows)
npm run build               # Сборка для разработки
npm run build:testing      # Сборка для тестирования
npm run build:production   # Сборка для продакшена

# Сборка (Unix/Linux/macOS)
npm run build:testing:unix     # Сборка для тестирования
npm run build:production:unix # Сборка для продакшена
```

### 📦 Процесс сборки

1. **Копирование конфигурации**: Скрипт копирует нужный файл конфигурации в `.env`
2. **Сборка**: React Scripts собирает приложение с нужными переменными
3. **Оптимизация**: Приложение оптимизируется для выбранного окружения

## Развертывание

### 🔄 Смена домена

Для смены домена достаточно обновить соответствующий файл конфигурации:

```bash
# Для тестирования
vim env.testing
# Изменить REACT_APP_API_URL на новый домен

# Для продакшена
vim env.production
# Изменить REACT_APP_API_URL на новый домен
```

### 🏗️ Сборка для продакшена

```bash
# Windows
npm run build:production

# Unix/Linux/macOS
npm run build:production:unix

# Результат будет в папке build/
```

### 🧪 Тестирование

```bash
# Запуск в режиме тестирования
npm run start:testing

# Сборка для тестирования (Windows)
npm run build:testing

# Сборка для тестирования (Unix)
npm run build:testing:unix
```

## Совместимость с операционными системами

### 🪟 Windows

Используйте команды с суффиксом без `:unix`:

```bash
npm run build:testing      # Windows
npm run build:production  # Windows
```

### 🐧 Unix/Linux/macOS

Используйте команды с суффиксом `:unix`:

```bash
npm run build:testing:unix     # Unix
npm run build:production:unix # Unix
```

### 🔧 Автоматическое определение

Для автоматического определения ОС можно использовать пакет `cross-env`:

```bash
# Установка
npm install --save-dev cross-env

# Использование в скриптах
"start:testing": "cross-env REACT_APP_TEST_MODE=true react-scripts start"
```

## Безопасность

### 🔒 Переменные окружения

- **Только переменные с префиксом `REACT_APP_`** доступны в браузере
- **Секретные данные** не должны быть в переменных окружения
- **Токены и ключи** должны быть на сервере

### 🛡️ Валидация конфигурации

```typescript
// Проверка обязательных переменных
if (!config.apiBaseUrl) {
  throw new Error('API URL is not configured');
}
```

## Отладка

### 🔍 Логирование

В режиме разработки включено подробное логирование:

```typescript
log('Checking user registration with initData:', initData);
log('User info received:', response.data);
log('Error checking user registration:', err);
```

### 🐛 Режим тестирования

Включение режима тестирования:

```bash
REACT_APP_TEST_MODE=true npm start
```

## Примеры использования

### 🌐 Разные домены

```typescript
// Локальная разработка
REACT_APP_API_URL=http://localhost:8080

// Тестовый сервер
REACT_APP_API_URL=https://api-test.youdovezu.com

// Продакшен
REACT_APP_API_URL=https://api.youdovezu.com
```

### 🔧 Настройка для команды

```bash
# Копирование примера конфигурации
cp env.example .env

# Редактирование под нужды команды
vim .env
```

## Заключение

Конфигурационная система YouDovezu обеспечивает:

- ✅ **Гибкость** - легко менять настройки для разных окружений
- ✅ **Безопасность** - правильное управление переменными окружения
- ✅ **Удобство** - простые команды для сборки и развертывания
- ✅ **Отладка** - подробное логирование в режиме разработки
- ✅ **Масштабируемость** - готовность к развертыванию на разных доменах
