# Функциональность веб-приложения YouDovezu

## Обзор

Веб-приложение YouDovezu предоставляет пользователям возможность искать и предлагать поездки через Telegram WebApp. Приложение адаптировано для мобильных устройств и интегрировано с Telegram ботом.

## Логика отображения страниц

### 1. Проверка статуса регистрации

Приложение автоматически проверяет статус регистрации пользователя:

```typescript
// Проверка статуса регистрации
if (!userInfo.isRegistered) {
  // Показываем страницу завершения регистрации
  return <RegistrationRequiredPage />;
}

// Показываем главное меню только полностью зарегистрированным пользователям
return <MainMenuPage />;
```

### 2. Условное отображение кнопок

Кнопки отображаются в зависимости от возможностей пользователя:

- **"Ищу машину"** - показывается только если `userInfo.capabilities.canSearchTrips === true`
- **"Предложить машину"** - показывается только если `userInfo.capabilities.canCreateTrips === true`

## Страницы приложения

### 🏠 Главное меню

**Отображается:** Только полностью зарегистрированным пользователям

**Содержит:**
- Приветствие с именем пользователя
- Кнопки "Ищу машину" и "Предложить машину" (в зависимости от прав)
- Статус пользователя (Пассажир/Водитель/Триал)

### 🔍 Страница поиска поездок

**Доступ:** Только пользователям с правом `canSearchTrips`

**Функции:**
- Форма поиска поездок:
  - Откуда (город отправления)
  - Куда (город назначения)
  - Дата поездки
  - Количество пассажиров
- Кнопка "Найти поездки"
- Область для отображения результатов поиска

### 🚙 Страница предложения поездки

**Доступ:** Только пользователям с правом `canCreateTrips`

**Функции:**
- Форма создания поездки:
  - Откуда (город отправления)
  - Куда (город назначения)
  - Дата поездки
  - Время отправления
  - Цена за место (в рублях)
  - Количество свободных мест
  - Комментарий к поездке
- Кнопка "Создать поездку"

### ⚠️ Страница завершения регистрации

**Отображается:** Пользователям, которые не завершили регистрацию

**Содержит:**
- Объяснение необходимости завершить регистрацию
- Пошаговые инструкции:
  1. Открыть бота @YoudovezuBot
  2. Выполнить команду /start
  3. Согласиться с политикой конфиденциальности
  4. Подтвердить номер телефона
- Кнопка "Закрыть" для закрытия веб-приложения

## Навигация

### Переходы между страницами

```typescript
const [currentView, setCurrentView] = useState<'main' | 'search' | 'offer'>('main');

// Переход к поиску поездок
const handleSearchTrips = () => {
  setCurrentView('search');
};

// Переход к предложению поездки
const handleOfferTrip = () => {
  setCurrentView('offer');
};

// Возврат в главное меню
const handleBackToMain = () => {
  setCurrentView('main');
};
```

### Кнопка "Назад"

На каждой странице (кроме главного меню) есть кнопка "← Назад" для возврата в главное меню.

## Безопасность

### Проверка авторизации

Каждый запрос к API проверяется через Telegram WebApp initData:

```typescript
// Получение initData от Telegram
let initData = '';
if (window.Telegram?.WebApp?.initData) {
  initData = window.Telegram.WebApp.initData;
}

// Отправка POST запроса с initData
const formData = new FormData();
formData.append('initData', initData);
const response = await axios.post('/api/webapp/user', formData);
```

### Обработка ошибок

- **401 Unauthorized** - показывается сообщение об ошибке авторизации
- **Другие ошибки** - показывается общее сообщение об ошибке
- **Кнопка "Попробовать снова"** для повторной попытки

## Адаптивный дизайн

### Мобильная оптимизация

- **Максимальная ширина:** 400px для удобства на мобильных устройствах
- **Адаптивные отступы:** Уменьшаются на маленьких экранах
- **Крупные кнопки:** Оптимизированы для касания пальцем
- **Читаемые шрифты:** Минимальный размер 14px

### CSS Grid и Flexbox

```css
.menu-buttons {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.page-header {
  display: flex;
  align-items: center;
}
```

## Тестирование

### В Telegram WebApp

Приложение автоматически получает initData от Telegram и работает с реальными данными пользователя.

### Для разработки

Можно передать тестовые данные через URL параметр:

```
http://localhost:3000?initData=test_data
```

Или использовать заглушку в коде для тестирования.

## Будущие улучшения

### Планируемая функциональность

1. **Результаты поиска** - отображение найденных поездок
2. **Создание поездок** - сохранение предложений в базу данных
3. **Фильтры поиска** - по цене, времени, типу автомобиля
4. **Профиль пользователя** - редактирование данных
5. **История поездок** - просмотр прошлых поездок
6. **Уведомления** - о новых предложениях и запросах

### Технические улучшения

1. **Состояние форм** - валидация и сохранение данных
2. **Загрузка данных** - индикаторы загрузки
3. **Кэширование** - сохранение данных локально
4. **Офлайн режим** - работа без интернета
5. **Push уведомления** - через Telegram API

## Заключение

Веб-приложение YouDovezu предоставляет удобный интерфейс для поиска и предложения поездок, интегрированный с Telegram. Приложение адаптировано для мобильных устройств и обеспечивает безопасный доступ только зарегистрированным пользователям с соответствующими правами.
