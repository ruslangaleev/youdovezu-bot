# Настройка GitHub Secrets для деплоя

Для работы автоматического деплоя необходимо настроить следующие секреты в GitHub.

## ⚠️ Важное предупреждение

**Все команды для создания SSH ключей выполняются на вашем ЛОКАЛЬНОМ компьютере, а не на сервере!**

- ✅ Правильно: Откройте терминал на своем компьютере и выполните команды
- ❌ Неправильно: Подключайтесь к серверу через SSH и выполняйте команды там

Если вы подключены к серверу через SSH, сначала выйдите командой `exit`, затем выполните команды на своем компьютере.

## Почему важно создавать ключи на локальной машине?

### 1. Принцип работы SSH ключей

SSH использует асимметричное шифрование:
- **Приватный ключ** - это ваш "ключ от двери", он должен быть там, **откуда вы подключаетесь**
- **Публичный ключ** - это "замок", он должен быть там, **куда вы подключаетесь** (на сервере)

```
Локальный компьютер          Сервер
┌─────────────────┐          ┌─────────────────┐
│ Приватный ключ  │─────────▶│ Публичный ключ  │
│ (ключ от двери) │          │ (замок)         │
│                 │          │                 │
│ Вы подключаетесь│          │ Разрешает вход  │
│ ОТСЮДА          │          │ СЮДА            │
└─────────────────┘          └─────────────────┘
```

### 2. GitHub Actions подключается к серверу

GitHub Actions запускается на серверах GitHub (не на вашем VPS), поэтому:
- Приватный ключ нужен **GitHub Actions** (хранится в GitHub Secrets)
- GitHub Actions использует приватный ключ для подключения **к вашему VPS серверу**
- Если ключ создан на сервере, GitHub Actions не сможет его использовать

```
GitHub Actions (GitHub серверы)
        │
        │ использует приватный ключ
        │ (из GitHub Secrets)
        ▼
┌─────────────────┐
│  Ваш VPS сервер │
│                 │
│ Публичный ключ  │
│ в authorized_keys│
└─────────────────┘
```

### 3. Безопасность

- **Приватный ключ** - это секретная информация, которая должна храниться в безопасном месте:
  - ✅ На вашем локальном компьютере
  - ✅ В GitHub Secrets (зашифровано)
  - ❌ НЕ на сервере (который может быть скомпрометирован)

- Если приватный ключ на сервере, злоумышленник, получивший доступ к серверу, сможет использовать его для подключения к другим серверам

### 4. Логика работы

**Правильный процесс:**
1. Создаете ключи на локальном компьютере
2. Публичный ключ копируете на сервер (в `authorized_keys`)
3. Приватный ключ добавляете в GitHub Secrets
4. GitHub Actions использует приватный ключ для подключения к серверу

**Неправильный процесс (если создать на сервере):**
1. Ключи созданы на сервере
2. Приватный ключ на сервере (GitHub Actions не может его использовать)
3. Нужно как-то передать приватный ключ с сервера в GitHub Secrets (небезопасно и неудобно)

### 5. Практический пример

**Если создать ключи на сервере:**
```bash
# На сервере (НЕПРАВИЛЬНО)
ssh user@server
ssh-keygen -t ed25519 -f ~/.ssh/github_actions_deploy
# Теперь приватный ключ на сервере, но GitHub Actions не может его использовать!
```

**Правильный способ:**
```bash
# На локальном компьютере (ПРАВИЛЬНО)
ssh-keygen -t ed25519 -f ~/.ssh/github_actions_deploy
# Приватный ключ на вашем компьютере
# Копируете публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub user@server
# Добавляете приватный ключ в GitHub Secrets
```

### Итог

**Приватный ключ = то, что подключается** → должен быть там, откуда идет подключение (локальный компьютер → GitHub Secrets)

**Публичный ключ = то, что разрешает подключение** → должен быть там, куда подключаются (на сервере в `authorized_keys`)

---

## Как добавить секреты в GitHub

### Вариант 1: Repository Secrets (для всех workflows)

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Введите имя секрета и его значение
5. Нажмите **Add secret**

### Вариант 2: Environment Secrets (рекомендуется для продакшена)

**Environments** позволяют организовать секреты и переменные по окружениям (Production, Staging, Development) и добавить дополнительные защиты (например, требовать одобрения перед деплоем).

**Создание Environment:**

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Environments**
3. Нажмите **New environment**
4. Введите имя: `Production`
5. Нажмите **Configure environment**

**Добавление секретов в Environment:**

1. В разделе **Environment secrets** нажмите **Add secret**
2. Введите имя секрета и его значение
3. Нажмите **Add secret**

**Добавление переменных в Environment:**

1. В разделе **Environment variables** нажмите **Add variable**
2. Введите имя переменной и её значение
3. Нажмите **Add variable**

**Преимущества Environments:**
- ✅ Организация секретов по окружениям
- ✅ Возможность добавить защиту (требовать одобрение перед деплоем)
- ✅ Разделение переменных (vars) и секретов (secrets)
- ✅ История изменений секретов

**Использование в workflow:**

Workflow автоматически использует Environment "Production" (указан в файле `.github/workflows/deploy-production.yml`):
- `${{ secrets.VPS_HOST }}` - берет из Environment secrets
- `${{ vars.VPS_DEPLOY_PATH }}` - берет из Environment variables

## Обязательные секреты

### 1. `VPS_HOST`
**Описание:** IP адрес или домен вашего VPS сервера

**Примеры:**
```
192.168.1.100
yourserver.com
vps.example.com
```

**Как получить:**
- IP адрес: обычно указан в панели управления вашего VPS провайдера
- Домен: если у вас настроен DNS для сервера

---

### 2. `VPS_USER`
**Описание:** Имя пользователя для SSH подключения к серверу

**Примеры:**
```
deploy
root
ubuntu
admin
```

**Рекомендации:**
- ✅ Используйте отдельного пользователя для деплоя (не root)
- ✅ Пользователь должен быть в группе `docker` для запуска контейнеров

**Как создать пользователя:**
```bash
# На сервере
sudo adduser deploy
sudo usermod -aG docker deploy
```

---

### 3. `VPS_SSH_PRIVATE_KEY`
**Описание:** Приватный SSH ключ для подключения к серверу

**Формат:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAABlwAAAAdzc2gtcn
...
(много строк)
...
-----END OPENSSH PRIVATE KEY-----
```

**Как создать SSH ключ:**

#### ⚠️ ВАЖНО: Выполняйте команды на вашем ЛОКАЛЬНОМ компьютере, НЕ на сервере!

**Шаг 1: Создание ключей на локальном компьютере**

Откройте терминал на вашем локальном компьютере (не на сервере!) и выполните:

```bash
# Генерация новой пары ключей
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy

# Или используйте RSA (если ed25519 не поддерживается)
ssh-keygen -t rsa -b 4096 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy
```

**Важно:** 
- При генерации ключа НЕ устанавливайте пароль (просто нажмите Enter)
- Ключи создаются на вашем локальном компьютере, а не на сервере!
- Если вы подключены к серверу через SSH, сначала выйдите командой `exit`

После выполнения команды у вас будет два файла **на вашем локальном компьютере**:
- `~/.ssh/github_actions_deploy` - **приватный ключ** (секретный, для GitHub)
- `~/.ssh/github_actions_deploy.pub` - **публичный ключ** (для сервера)

**Если вы случайно создали ключи на сервере:**

Если вы уже выполнили команду `ssh-keygen` на сервере (подключившись через SSH), у вас есть два варианта:

**Вариант 1: Использовать существующие ключи с сервера (быстро, но не идеально)**

Если публичный ключ уже на сервере (например, `~/.ssh/github_actions_deploy.pub`), добавьте его в `authorized_keys`:

```bash
# На сервере (вы уже подключены)
# Создайте папку .ssh если её нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ в authorized_keys
cat ~/.ssh/github_actions_deploy.pub >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/authorized_keys

# Проверьте, что ключ добавлен
cat ~/.ssh/authorized_keys
```

Затем скопируйте **приватный ключ** с сервера для добавления в GitHub Secrets:

```bash
# На сервере посмотрите приватный ключ
cat ~/.ssh/github_actions_deploy

# Скопируйте ВСЁ содержимое (от -----BEGIN до -----END)
# Затем добавьте его в GitHub Secrets как VPS_SSH_PRIVATE_KEY
```

**⚠️ Важно:** После копирования приватного ключа в GitHub Secrets, рекомендуется удалить его с сервера для безопасности:

```bash
# На сервере удалите приватный ключ (публичный можно оставить)
rm ~/.ssh/github_actions_deploy
```

**Вариант 2: Создать ключи заново на локальном компьютере (рекомендуется)**

1. **Выйти с сервера:**
   ```bash
   exit
   ```

2. **Удалить ключи на сервере (если хотите):**
   ```bash
   ssh user@your-server-ip
   rm ~/.ssh/github_actions_deploy*
   exit
   ```

3. **Создать ключи заново на локальном компьютере** (выполните команду выше на своем компьютере, не на сервере)

#### Что такое authorized_keys?

`authorized_keys` - это файл на вашем VPS сервере, который содержит список публичных SSH ключей, которым разрешено подключаться к серверу без пароля. Когда GitHub Actions пытается подключиться к серверу, сервер проверяет, есть ли публичный ключ в этом файле.

#### Копирование публичного ключа на сервер:

**⚠️ Выполняйте команды на вашем ЛОКАЛЬНОМ компьютере!**

**Вариант А: Автоматически (рекомендуется, если доступна команда ssh-copy-id):**

На вашем локальном компьютере выполните:

```bash
# Автоматическое копирование (самый простой способ)
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub user@your-server-ip
```

**Пример:**
```bash
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub deploy@192.168.1.100
```

**Что произойдет:**
- Команда попросит ввести пароль от сервера (в последний раз)
- Автоматически создаст папку `~/.ssh` на сервере (если её нет)
- Добавит ваш публичный ключ в файл `~/.ssh/authorized_keys`
- Установит правильные права доступа

**Вариант Б: Вручную (если ssh-copy-id не работает, например на Windows):**

**Шаг 1:** На вашем локальном компьютере посмотрите содержимое публичного ключа:

```bash
cat ~/.ssh/github_actions_deploy.pub
```

Вы увидите что-то вроде:
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIG... github-actions-deploy
```

**Скопируйте всю эту строку** (от `ssh-ed25519` до конца).

**Шаг 2:** На вашем локальном компьютере подключитесь к серверу:

```bash
ssh user@your-server-ip
```

**Пример:**
```bash
ssh deploy@192.168.1.100
```

Введите пароль для входа на сервер.

**Шаг 3:** На сервере создайте папку и файл (если их нет):

```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
```

**Шаг 4:** Добавьте публичный ключ в authorized_keys:

```bash
echo "ваш_публичный_ключ_из_шага_1" >> ~/.ssh/authorized_keys
```

**Пример:**
```bash
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIG... github-actions-deploy" >> ~/.ssh/authorized_keys
```

**Шаг 5:** Установите правильные права:

```bash
chmod 600 ~/.ssh/authorized_keys
```

**Шаг 6:** Выйдите с сервера:

```bash
exit
```

#### Проверка подключения:

Попробуйте подключиться к серверу **без пароля**:

```bash
ssh -i ~/.ssh/github_actions_deploy user@your-server-ip
```

**Пример:**
```bash
ssh -i ~/.ssh/github_actions_deploy deploy@192.168.1.100
```

**Если подключение прошло без запроса пароля** - всё настроено правильно! ✅

#### Получение приватного ключа для GitHub:

**Шаг 1:** Посмотрите содержимое приватного ключа:

```bash
cat ~/.ssh/github_actions_deploy
```

Вы увидите что-то вроде:
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAABlwAAAAdzc2gtcn
NhAAAAIwAAAAdzc2gtcnNhAAABBQ...
(много строк)
...
-----END OPENSSH PRIVATE KEY-----
```

**Шаг 2:** Скопируйте ВСЁ содержимое

**Важно:** Скопируйте всё от `-----BEGIN` до `-----END` включительно!

**Шаг 3:** Добавьте в GitHub Secrets:

1. Откройте ваш репозиторий на GitHub
2. Перейдите: **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Name: `VPS_SSH_PRIVATE_KEY`
5. Value: вставьте скопированный приватный ключ (весь текст)
6. Нажмите **Add secret**

**⚠️ ВАЖНО:**
- Никогда не коммитьте приватный ключ в репозиторий!
- Приватный ключ должен начинаться с `-----BEGIN` и заканчиваться `-----END`
- Скопируйте ключ полностью, включая все строки
- Не добавляйте лишних пробелов или переносов строк

#### Как это работает (визуальное объяснение):

```
┌─────────────────┐
│ Ваш компьютер   │
│                 │
│ Приватный ключ  │──┐
│ (секретный)     │  │
│                 │  │ SSH подключение
│ Публичный ключ  │──┼─────────────────┐
│ (открытый)      │  │                 │
└─────────────────┘  │                 │
                     │                 ▼
                     │         ┌─────────────────┐
                     │         │   VPS сервер     │
                     │         │                 │
                     │         │ authorized_keys │
                     └─────────│ (список         │
                               │  разрешенных     │
                               │  ключей)         │
                               └─────────────────┘
```

1. **Приватный ключ** остается на вашем компьютере и в GitHub Secrets
2. **Публичный ключ** копируется на сервер в файл `authorized_keys`
3. Когда GitHub Actions подключается к серверу, сервер проверяет: "Есть ли этот публичный ключ в authorized_keys?"
4. Если есть - доступ разрешен ✅

---

## Опциональные секреты

### 4. `VPS_DEPLOY_PATH`
**Описание:** Путь на сервере, куда будет развернут проект

**Значение по умолчанию:** `/opt/youdovezu-bot`

**Примеры:**
```
/opt/youdovezu-bot
/home/deploy/youdovezu
/var/www/youdovezu
```

**Когда использовать:**
- Если вы хотите развернуть проект в нестандартную директорию
- Если у вас несколько проектов на одном сервере

**Если не указан:**
- Workflow автоматически использует `/opt/youdovezu-bot`

---

## Проверка настройки

После добавления всех секретов проверьте:

1. ✅ Все 3 обязательных секрета добавлены
2. ✅ SSH ключ скопирован полностью (включая BEGIN/END строки)
3. ✅ Публичный ключ добавлен на сервер в `~/.ssh/authorized_keys`
4. ✅ Пользователь может подключаться по SSH без пароля
5. ✅ Пользователь может запускать Docker команды

### Тест SSH подключения:

```bash
# На вашем локальном компьютере
ssh -i ~/.ssh/github_actions_deploy user@your-server-ip

# Если подключение успешно - секреты настроены правильно
```

### Тест Docker доступа:

```bash
# На сервере (под пользователем для деплоя)
docker ps
docker-compose --version

# Если команды работают без sudo - всё настроено правильно
```

---

## Безопасность

### ✅ Рекомендации:

1. **Используйте отдельного пользователя** для деплоя (не root)
2. **Ограничьте SSH доступ** по IP, если возможно
3. **Используйте ed25519 ключи** (более безопасные, чем RSA)
4. **Регулярно ротируйте ключи** (меняйте раз в 3-6 месяцев)
5. **Не коммитьте ключи** в репозиторий
6. **Используйте сильные пароли** для пользователей на сервере

### ❌ Чего НЕ делать:

- ❌ Не используйте root пользователя для деплоя
- ❌ Не коммитьте приватные ключи в git
- ❌ Не делитесь приватными ключами
- ❌ Не используйте слабые SSH ключи (меньше 2048 бит для RSA)

---

## Устранение проблем

### Проблема: "Permission denied (publickey)"

**Решение:**

**Шаг 1:** Проверьте, что публичный ключ добавлен на сервер:

```bash
# Подключитесь к серверу
ssh user@your-server-ip

# Проверьте содержимое authorized_keys
cat ~/.ssh/authorized_keys
```

Должна быть строка с вашим публичным ключом (начинается с `ssh-ed25519` или `ssh-rsa`).

**Шаг 2:** Если ключа нет, добавьте его вручную:

```bash
# На сервере
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Скопируйте ваш публичный ключ с локального компьютера
# (выполните на локальном компьютере)
cat ~/.ssh/github_actions_deploy.pub

# Затем на сервере добавьте его:
echo "ваш_публичный_ключ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

**Шаг 3:** Проверьте права на файлы на сервере:

```bash
# На сервере
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys

# Проверьте права
ls -la ~/.ssh/
# Должно быть:
# -rw------- authorized_keys (права 600)
# drwx------ . (права 700)
```

**Шаг 4:** Убедитесь, что приватный ключ скопирован полностью в GitHub Secrets (включая строки `-----BEGIN` и `-----END`)

**Шаг 5:** Проверьте подключение с локального компьютера:

```bash
# На локальном компьютере
ssh -i ~/.ssh/github_actions_deploy user@your-server-ip
```

Если подключение прошло без пароля - проблема решена ✅

### Проблема: "Host key verification failed"

**Решение:**
- Workflow автоматически добавляет хост в known_hosts, но если проблема сохраняется:
  ```bash
  ssh-keyscan -H your-server-ip >> ~/.ssh/known_hosts
  ```

### Проблема: "docker: permission denied"

**Решение:**

```bash
# На сервере добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Выйдите и войдите снова, или выполните:
newgrp docker

# Проверьте, что команды работают без sudo
docker ps
docker-compose --version
```

### Проблема: "ssh-copy-id: command not found" (Windows)

**Решение:** Используйте ручной способ (Вариант Б) из раздела "Копирование публичного ключа на сервер" выше.

### Проблема: Ключ не работает после копирования

**Решение:**

1. **Проверьте, что скопировали публичный ключ полностью** (одну строку от начала до конца)
2. **Проверьте права на файлы на сервере:**
   ```bash
   ssh user@your-server-ip
   ls -la ~/.ssh/
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```
3. **Убедитесь, что используете правильного пользователя** (тот же, что указан в `VPS_USER`)
4. **Проверьте, что ключ добавлен в правильный файл:**
   ```bash
   # На сервере
   cat ~/.ssh/authorized_keys | grep "github-actions"
   ```
   Должна быть строка с вашим ключом

---

## Пример полной настройки

### Шаг 1: Генерация ключей
```bash
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_deploy
```

### Шаг 2: Копирование на сервер
```bash
ssh-copy-id -i ~/.ssh/github_deploy.pub deploy@192.168.1.100
```

### Шаг 3: Получение приватного ключа
```bash
cat ~/.ssh/github_deploy
# Скопируйте весь вывод
```

### Шаг 4: Добавление в GitHub
1. Settings → Secrets → Actions → New repository secret
2. Name: `VPS_SSH_PRIVATE_KEY`
3. Value: (вставьте скопированный ключ)
4. Add secret

### Шаг 5: Добавление остальных секретов
- `VPS_HOST`: `192.168.1.100`
- `VPS_USER`: `deploy`
- `VPS_DEPLOY_PATH`: `/opt/youdovezu-bot` (опционально)

---

## Готово! 🎉

После настройки всех секретов workflow будет автоматически развертывать проект при каждом push в ветку `main` или `master`.

