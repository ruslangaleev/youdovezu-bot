# Управление запросами на поездки

## Общее описание

Управление запросами на поездки позволяет пользователям создавать, просматривать, редактировать и удалять свои запросы на поездки. Пользователь может публиковать запросы на поездки с указанием маршрута от пункта отправления до пункта назначения и ожидать предложения от попутчиков, которые предоставляют машину.

## Страница "Мои поездки"

### Описание

Страница отображает список всех запросов на поездки, созданных текущим пользователем. Пользователь может управлять своими запросами: создавать новые, редактировать существующие и удалять их.

### Навигация

- **Кнопка "← Назад"** — возвращает в главное меню
- Открывается из главного меню при нажатии на кнопку "Ищу машину"

### Состояния страницы

**Загрузка:**
- Показывается индикатор загрузки (спиннер)
- Текст "Загрузка поездок..."
- Страница не доступна для взаимодействия

**Пустой список:**
- Отображается сообщение "У вас пока нет поездок"
- Подсказка "Создайте первый запрос на поездку, чтобы найти попутчика, который предоставляет машину"
- Кнопка "➕ Новая поездка" для создания первого запроса

**Список запросов:**
- Каждый запрос отображается в виде карточки
- Запросы отсортированы по дате создания (новые сверху)
- Внизу страницы кнопка "➕ Новая поездка"

### Информация о запросе

Каждый запрос в списке содержит:

**Маршрут:**
- "Откуда: [адрес] [населенный пункт]"
- Стрелка → 
- "Куда: [адрес] [населенный пункт]"

**Комментарий (если указан):**
- "Комментарий: [текст комментария]"

**Дата создания:**
- "Создано: [дата и время в локальном часовом поясе]"
- Формат: "Создано: [день] [месяц] [год], [часы]:[минуты]"

**Статус поездки:**
- "Активна" — поездка активна и доступна
- "Закрыта" — поездка завершена или отменена
- Цветовая индикация статуса

**Кнопки управления:**
- "✏️ Редактировать" — открывает страницу редактирования запроса
- "🗑️ Удалить" — удаляет запрос (с подтверждением)

### Действия на странице

**Кнопка "➕ Новая поездка":**
- Открывает страницу создания новой поездки
- Доступна всегда, даже если список пуст

**Кнопка "✏️ Редактировать":**
- Открывает страницу редактирования выбранного запроса
- Форма предзаполняется данными текущего запроса
- Доступна только для активных запросов

**Кнопка "🗑️ Удалить":**
- Показывает подтверждение удаления
- После подтверждения запрос удаляется
- Список запросов обновляется автоматически
- Показывает "🔄 Удаление..." во время процесса

## Создание нового запроса на поездку

### Описание

Страница для создания нового запроса на поездку с указанием маршрута и дополнительной информации. После создания запрос публикуется, и попутчики, которые предоставляют машину, могут просматривать его и предлагать свои услуги.

### Навигация

- **Кнопка "← Назад"** — возвращает на страницу "Мои поездки"
- Несохраненные данные теряются при возврате

### Поля формы

**Населенный пункт (Откуда):**
- Выпадающий список с доступными населенными пунктами
- Обязательное поле
- Сначала нужно выбрать населенный пункт, затем адрес

**Адрес (Откуда):**
- Текстовое поле с автодополнением адресов
- Обязательное поле
- Доступно только после выбора населенного пункта
- При вводе показываются подсказки адресов (только в пределах Республики Башкортостан)
- Кнопка "🗺️ Показать на карте" становится активной после выбора адреса
- Кнопка "✕" для очистки поля

**Населенный пункт (Куда):**
- Выпадающий список с доступными населенными пунктами
- Обязательное поле

**Адрес (Куда):**
- Текстовое поле с автодополнением адресов
- Обязательное поле
- Работает аналогично полю "Адрес (Откуда)"

**Комментарий:**
- Многострочное текстовое поле
- Необязательное поле
- Для дополнительной информации о поездке

### Автодополнение адресов

**Как работает:**
- При вводе адреса система автоматически предлагает варианты адресов
- Подсказки ограничены только адресами в Республике Башкортостан
- Каждая подсказка показывает:
  - Основной заголовок: название улицы или объекта
  - Описание: полный адрес с указанием региона, населенного пункта

**При выборе подсказки:**
- Адрес заполняется в поле
- Сохраняются координаты (широта и долгота)
- Сохраняется полный адрес для отображения на карте
- Кнопка "🗺️ Показать на карте" становится активной

### Кнопки

**Кнопка "🚙 Создать поездку":**
- Создает запрос на поездку и сохраняет его в системе
- Запрос публикуется и становится доступным для просмотра попутчикам, которые предоставляют машину
- Во время создания показывает "🔄 Создание..." и становится неактивной
- После успешного создания:
  - Показывается уведомление об успехе
  - Пользователь перенаправляется на страницу "Мои поездки"
  - Список запросов обновляется автоматически

**Кнопка "🗺️ Показать на карте":**
- Доступна только для адресов, выбранных из подсказок
- Открывает Яндекс.Карты в новой вкладке с выбранным адресом
- Используется полный адрес, включая регион и населенный пункт

### Валидация

- При попытке создать поездку без обязательных полей показывается сообщение об ошибке
- Обязательные поля: населенные пункты отправления и назначения, адреса отправления и назначения
- Комментарий не является обязательным полем

## Редактирование запроса на поездку

### Описание

Страница для редактирования существующего запроса на поездку. Идентична странице создания, но с предзаполненными данными.

### Навигация

- Открывается при нажатии "✏️ Редактировать" на поездке
- **Кнопка "← Назад"** — возвращает на страницу "Мои поездки"
- Изменения не сохраняются при возврате

### Особенности

- Все поля формы заполнены данными текущей поездки
- Пользователь может изменить любые поля
- Автодополнение адресов работает так же, как при создании
- Кнопка "🗺️ Показать на карте" доступна для уже выбранных адресов

### Кнопки

**Кнопка "💾 Сохранить изменения":**
- Сохраняет изменения в запросе
- Во время сохранения показывает "🔄 Сохранение..." и становится неактивной
- После успешного сохранения:
  - Показывается уведомление об успехе
  - Пользователь перенаправляется на страницу "Мои поездки"
  - Список запросов обновляется

### Ограничения

- Редактировать можно только свои запросы
- Пользователь не может редактировать запросы других пользователей
- Можно редактировать только активные запросы

## Удаление запроса на поездку

### Процесс удаления

1. Пользователь нажимает кнопку "🗑️ Удалить" на запросе
2. Система показывает подтверждение:
   - В Telegram WebApp: нативное диалоговое окно подтверждения
   - В браузере: стандартное окно подтверждения
   - Текст: "Вы уверены, что хотите удалить этот запрос на поездку?"
3. Если пользователь подтверждает:
   - Запрос помечается как удаленный
   - Запрос больше не отображается в списке
   - Запрос больше не доступен для просмотра попутчикам, которые предоставляют машину
   - Показывается уведомление об успешном удалении
   - Список запросов обновляется автоматически
4. Если пользователь отменяет:
   - Операция отменяется
   - Запрос остается без изменений

### Особенности удаления

- Удаление является "мягким" — запрос не удаляется физически, а только помечается как удаленный
- Удаленные запросы не отображаются в списке
- Удаленные запросы больше не доступны для просмотра попутчикам, которые предоставляют машину
- Удалить можно только свои запросы
- Удаление необратимо для пользователя (нельзя восстановить)

## Данные запроса на поездку

### Обязательные данные

**Маршрут:**
- Населенный пункт отправления
- Адрес отправления
- Населенный пункт назначения
- Адрес назначения

### Дополнительные данные

**Комментарий:**
- Произвольный текст
- Дополнительная информация о поездке
- Необязательное поле

**Координаты:**
- Широта и долгота для точки отправления
- Широта и долгота для точки назначения
- Сохраняются автоматически при выборе адреса из подсказок
- Используются для отображения на карте

### Системные данные

**Дата создания:**
- Автоматически устанавливается при создании
- Отображается пользователю в локальном часовом поясе
- Формат: "Создано: [дата] [время]"

**Дата обновления:**
- Автоматически обновляется при изменении поездки
- Используется системой для отслеживания изменений

**Статус:**
- Активна, Закрыта или Удалена
- Отображается пользователю в списке поездок

## Ограничения и правила

### Географические ограничения

- Поиск адресов ограничен только Республикой Башкортостан
- Адреса за пределами региона не предлагаются в подсказках
- Пользователь может указать только адреса в пределах региона

### Права доступа

- Пользователь может создавать, редактировать и удалять только свои поездки
- Пользователь не может изменять поездки других пользователей
- Для использования функций необходимо завершить регистрацию

### Управление запросами

- Любой зарегистрированный пользователь может создавать запросы на поездки
- Не требуется проверка документов для создания запроса на поездку
- Можно редактировать только активные запросы
- После публикации запрос становится доступным для просмотра попутчикам, которые предоставляют машину

### Отображение данных

- Даты отображаются в локальном часовом поясе пользователя
- Список запросов отсортирован по дате создания (новые сверху)
- Удаленные запросы не отображаются в списке

