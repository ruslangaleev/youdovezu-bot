# YouDovezu

P2P-платформа для организации поездок (не такси) в Башкирии (Караидель, Уфа, Бирск и др.). Пользователи публикуют запросы на поездки, водители предлагают цены, клиенты выбирают. Сервис работает через Telegram-бот (@YoudovezuBot) с веб-приложением.

## Описание проекта

**YouDovezu** — это "доска объявлений" для поездок, где пользователи договариваются напрямую, платформа только соединяет.

### Целевая аудитория

- **Клиенты**: Жители Башкирии, которым нужны попутчики (город/село, межгород)
- **Водители**: Частные лица с авто, готовые подвозить за плату
- **Админы**: Проверяют фото прав/СТС водителей, могут быть клиентами/водителями

### Цель MVP

- Запустить Telegram-бота с базовыми функциями: регистрация, запросы, предложения, рейтинги, триал (14 дней), поддержка
- Проверить гипотезу: есть спрос на P2P-поездки в Башкирии (50+ юзеров)
- Подготовить архитектуру для масштабирования (VK, новые регионы)

## Архитектура

Проект построен по принципам чистой архитектуры (Clean Architecture) и состоит из следующих слоев:

- **YouDovezu.Domain** - Слой домена с сущностями и бизнес-логикой
- **YouDovezu.Application** - Слой приложения с интерфейсами сервисов и репозиториев
- **YouDovezu.Infrastructure** - Слой инфраструктуры с реализацией репозиториев и контекстом БД
- **YouDovezu.API** - Слой представления с контроллерами и конфигурацией

## Структура проекта

```
youdovezu-bot/
├── backend/                    # Серверная часть
│   ├── src/
│   │   ├── YouDovezu.Domain/   # Слой домена
│   │   │   └── Entities/       # Сущности: User, TripRequest, TripOffer, Rating, DriverInfo
│   │   ├── YouDovezu.Application/ # Слой приложения
│   │   │   ├── Commands/       # Команды CQRS
│   │   │   ├── Queries/        # Запросы CQRS
│   │   │   ├── Handlers/       # Обработчики команд и запросов
│   │   │   └── Common/         # Общие интерфейсы и модели
│   │   ├── YouDovezu.Infrastructure/ # Слой инфраструктуры
│   │   │   └── Data/           # Контекст базы данных
│   │   └── YouDovezu.API/      # Слой представления
│   │       └── Controllers/    # API контроллеры
│   ├── YouDovezu.sln          # Solution файл
│   └── Dockerfile             # Docker для backend
├── frontend/                   # Клиентская часть
│   └── client/
│       ├── src/
│       │   ├── App.tsx         # Главный компонент
│       │   ├── App.css        # Стили
│       │   └── index.tsx      # Точка входа
│       ├── public/
│       └── Dockerfile         # Docker для frontend
├── docker-compose.yml         # Оркестрация сервисов
└── README.md
```

## Технологии

### Backend
- .NET 9
- ASP.NET Core Web API
- Entity Framework Core
- PostgreSQL
- MediatR (CQRS pattern)

### Frontend
- React 18
- TypeScript
- CSS3

### DevOps
- Docker & Docker Compose

## Доменные сущности

### User (Пользователь)
- Основная информация: имя, email, телефон
- Telegram данные: TelegramId, TelegramUsername
- Роли: User, Driver, Admin
- Согласие на обработку ПД: PdConsent, PdConsentAt
- Триал период: TrialStart, TrialEnd
- Верификация: IsVerified
- Рейтинг: Rating, RatingCount

### TripRequest (Запрос на поездку)
- Маршрут: FromAddress, ToAddress, координаты
- Описание и количество пассажиров
- Статус: Active, Accepted, Completed, Cancelled
- Предпочтительное время
- Принятое предложение

### TripOffer (Предложение водителя)
- Связь с запросом и водителем
- Цена и сообщение
- Статус: Pending, Accepted, Rejected, Cancelled

### Rating (Рейтинг)
- Оценка от пользователя к пользователю
- Звезды (1-5) и комментарий
- Тип: ClientToDriver, DriverToClient

### DriverInfo (Информация о водителе)
- Данные прав и автомобиля
- Фото документов
- Статус верификации админом

## Бизнес-логика

### Основные сценарии

#### Регистрация
1. Пользователь запускает @YoudovezuBot (/start)
2. Бот отправляет Политику конфиденциальности (ПД, ФЗ-152) и кнопку "Согласен"
3. После согласия подтверждает номер телефона (из Telegram initData)
4. Создается пользователь (роль User, PdConsent=true, TrialStart/TrialEnd для будущих водителей)
5. Бот отправляет кнопку Web App

#### Водитель
1. В Web App: Регистрация как водитель (загрузка прав/СТС, марка/цвет/номер авто)
2. Админ проверяет фото вручную (1-2 дня), подтверждает (IsVerified=true)
3. Тестовый период: 14 дней бесплатно, затем 100 руб/день или 1000 руб/мес

#### Запрос на поездку
1. Клиент в Web App публикует запрос (откуда, куда, описание)
2. Yandex Maps API: автодополнение адресов, геокодирование (lat/lon)
3. Запрос виден водителям в радиусе ~50 км

#### Предложения
1. Водитель в Web App предлагает цену и сообщение
2. Клиент получает пуш, выбирает предложение
3. После выбора: пуш водителю, статус запроса меняется на Accepted

#### Рейтинг
1. После поездки клиент/водитель ставят оценку (1-5 звёзд, комментарий)
2. Рейтинг отображается в профиле

## Быстрый старт

### Запуск с Docker Compose (рекомендуется)

```bash
docker-compose up --build
```

**Доступные сервисы:**
- **React Frontend:** `http://localhost:3000`
- **API Backend:** `http://localhost:5000`
- **Swagger UI:** `http://localhost:5000/swagger`
- **PostgreSQL:** `localhost:5432`

### Запуск локально

#### Backend (API)
1. Установите .NET 9 SDK
2. Установите PostgreSQL
3. Обновите строку подключения в `backend/src/YouDovezu.API/appsettings.json`
4. Запустите API:

```bash
cd backend
dotnet run --project src/YouDovezu.API
```

#### Frontend (React)
1. Установите Node.js 18+
2. Запустите React приложение:

```bash
cd frontend/client
npm install
npm start
```

## Функциональность

### Текущие возможности
- ✅ Минимальный каркас API
- ✅ Минимальный каркас React приложения
- ✅ Docker контейнеризация
- ✅ Чистая архитектура
- ✅ TypeScript поддержка
- ✅ Health Check endpoint
- ✅ MediatR интеграция (CQRS)
- ✅ Доменные сущности для P2P-платформы
- ✅ Команды для регистрации пользователей
- ✅ Команды для запросов на поездки
- ✅ Команды для предложений водителей
- ✅ Система рейтингов

### Планируемые возможности
- 🔄 Telegram Bot интеграция
- 🔄 Web App для клиентов и водителей
- 🔄 Yandex Maps API интеграция
- 🔄 Система уведомлений
- 🔄 Платежная система (ЮKassa)
- 🔄 Админ панель для верификации
- 🔄 VK Bot интеграция

## API Endpoints

### Health Check
- **GET** `/api/health` - Проверка состояния API

### Пользователи
- **POST** `/api/users/register-telegram` - Регистрация через Telegram

### Поездки
- **POST** `/api/trips/request` - Создание запроса на поездку
- **POST** `/api/trips/offer` - Создание предложения водителя
- **POST** `/api/trips/accept-offer` - Принятие предложения

### Рейтинги
- **POST** `/api/ratings` - Создание рейтинга

## Разработка

### Добавление новых API endpoints с MediatR

1. Создайте команду/запрос в `backend/src/YouDovezu.Application/Commands/` или `Queries/`
2. Создайте обработчик в `backend/src/YouDovezu.Application/Handlers/`
3. Добавьте endpoint в контроллер в `backend/src/YouDovezu.API/Controllers/`
4. MediatR автоматически зарегистрирует обработчики

### Добавление новых компонентов React

1. Создайте компонент в `frontend/client/src/components/`
2. Импортируйте и используйте компонент в `App.tsx`

## Лицензия

MIT License