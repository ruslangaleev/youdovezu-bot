# YouDovezu

P2P-платформа для организации поездок в Башкирии. Пользователи публикуют запросы на поездки, водители предлагают цены, клиенты выбирают. Сервис работает через Telegram-бот (@YoudovezuBot) с веб-приложением.

## 🚀 Быстрый запуск

### Переменные окружения для Docker Compose

Скопируйте файл `env.example` в `.env` и заполните реальными значениями:

```bash
cp env.example .env
```

Основные переменные:
- `POSTGRES_DB` - имя базы данных
- `POSTGRES_USER` - пользователь PostgreSQL  
- `POSTGRES_PASSWORD` - пароль PostgreSQL
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `WEB_APP_URL` - URL веб-приложения

### Запуск проекта

```bash
# Клонируйте репозиторий
git clone https://github.com/ruslangaleev/youdovezu-bot.git
cd youdovezu-bot

# Запустите все сервисы
docker-compose up --build
```

## 📱 Доступные ссылки

После запуска будут доступны:

- **🌐 React Frontend:** [http://localhost:3000](http://localhost:3000)
- **🔧 API Backend:** [http://localhost:5000](http://localhost:5000)
- **📚 Swagger UI:** [http://localhost:5000/swagger](http://localhost:5000/swagger)
- **🗄️ PostgreSQL:** `localhost:5432`

## 🏗️ Архитектура

Проект построен по принципам чистой архитектуры:

- **Domain** - Бизнес-сущности и правила
- **Application** - Use cases, команды, запросы (CQRS с MediatR)
- **Infrastructure** - Внешние сервисы, база данных
- **API** - HTTP контроллеры

## 🛠️ Технологии

- **Backend:** .NET 9, ASP.NET Core, Entity Framework Core, PostgreSQL, MediatR
- **Frontend:** React 18, TypeScript
- **DevOps:** Docker, Docker Compose

## 📋 Основные функции

- ✅ Регистрация через Telegram бота
- ✅ Согласие с политикой конфиденциальности (GDPR)
- ✅ Подтверждение номера телефона
- ✅ Система запросов и предложений поездок
- ✅ Рейтинговая система
- ✅ Триал период (14 дней)
- ✅ Верификация водителей

## 🔧 Разработка

### Локальный запуск (без Docker)

#### Backend
```bash
cd backend
dotnet run --project src/YouDovezu.API
```

#### Frontend
```bash
cd frontend/client
npm install
npm start
```

### Структура проекта
```
youdovezu-bot/
├── backend/src/           # .NET API
├── frontend/client/        # React приложение
├── docker-compose.yml     # Оркестрация сервисов
├── env.example            # Пример переменных окружения
└── .env                   # Переменные окружения (создать из env.example)
```

## 🤖 Настройка Telegram Bot

### Получение токена бота
1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### Настройка Webhook
После запуска проекта настройте webhook для получения обновлений:

```bash
# Замените YOUR_TOKEN на токен вашего бота
# Замените YOUR_WEBHOOK_URL на URL вашего API + /api/telegram/webhook
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/setWebhook?url=<YOUR_WEBHOOK_URL>"
```

**Пример:**
```bash
curl -X POST "https://api.telegram.org/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz/setWebhook?url=https://yourdomain.com/api/telegram/webhook"
```

### Проверка webhook
```bash
curl -X GET "https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo"
```

## 📞 Поддержка

- **Email:** support@youdovezu.ru
- **Telegram:** @YoudovezuBot

## 📄 Лицензия

MIT License